function PrintNumbers()
{
        for(i=1;i<=100;i++)
        {
                if(i%3===0)
                        Console.Log("Earth")
                 if(i%5===0)
                        Console.Log("Jupiter")        
                if(i%9===0)
                        Console.Log("Pluto")                
                Console.Log(i)

        }

} 
